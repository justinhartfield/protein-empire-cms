# ===========================================
# Protein Empire CMS - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values
# For Railway: Most of these are auto-configured

# Server Configuration
HOST=0.0.0.0
PORT=1337
PUBLIC_URL=https://your-app.up.railway.app

# Database Configuration
# For Railway PostgreSQL, just set DATABASE_CLIENT=postgres
# Railway auto-injects DATABASE_URL
DATABASE_CLIENT=postgres
# DATABASE_URL is auto-injected by Railway when you add PostgreSQL

# For local development with SQLite:
# DATABASE_CLIENT=sqlite
# DATABASE_FILENAME=.tmp/data.db

# Security Keys (REQUIRED - generate new ones for production!)
# Generate with: openssl rand -base64 16
APP_KEYS=key1==,key2==,key3==,key4==
API_TOKEN_SALT=your-api-token-salt==
ADMIN_JWT_SECRET=your-admin-jwt-secret==
TRANSFER_TOKEN_SALT=your-transfer-token-salt==
JWT_SECRET=your-jwt-secret==
ENCRYPTION_KEY=your-encryption-key==

# Optional: Cloudinary for media storage (recommended for production)
# CLOUDINARY_NAME=your-cloud-name
# CLOUDINARY_KEY=your-api-key
# CLOUDINARY_SECRET=your-api-secret

# Optional: SMTP for email
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USERNAME=your-username
# SMTP_PASSWORD=your-password
